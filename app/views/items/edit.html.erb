<%= form_for [@business, @item], url: { action: "update" } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: :name %>
  <%= f.label :description %>
  <%= f.text_field :description, placeholder: :description %>
  <%= f.submit %>
<% end %>
